# CDI Playground - inject-primitive

To navigate:

    `cd inject-primitive`

To build:

    `mvn clean install`

To run:

    `java -jar target/inject-primitive1.0.jar Main`

Expected Output:

```

```
